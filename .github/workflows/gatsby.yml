name: Gatsby Publish

on:
  push:
    branches:
      - gh-pages-dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: enriikke/gatsby-gh-pages-action@v2.2.0
        with:
          access-token: ${{ secrets.SITE_DEPLOY_ACCESS_TOKEN }}